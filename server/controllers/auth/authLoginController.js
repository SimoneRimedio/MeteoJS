const { Request, Response } = require("express");
const Prisma = require("../../database/connection.js");
const jwt = require("jsonwebtoken");

const login = async (req, res) => {
  
};

module.exports = login;
